{
  "scripts": {
    "dev": "nodemon index.js",
    "test": "jest --watch --verbose",
    "coverage": "jest --watch --verbose --coverage"
  },
  "dependencies": {
    "express": "^4.18.1"
  },
  "devDependencies": {
    "cors": "^2.8.5",
    "jest": "^28.1.3",
    "nodemon": "^2.0.19",
    "supertest": "^6.2.4"
  }
}
